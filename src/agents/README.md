# Agents Layer

LangGraph-based multi-agent workflows for prompt optimization.

## Structure
- `graph/` — LangGraph state machines and workflow definitions
- `nodes/` — Individual agent nodes (analyzer, compressor, optimizer, validator)
- `state/` — Shared agent state types
- `tools/` — LangChain tools available to agents
